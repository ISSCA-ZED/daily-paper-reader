---
title: "WaveFilter: Enhancing the Long-Context Capability of Diffusion LLMs via Wavelet-Guided KV Cache Filtering"
title_zh: WaveFilter：通过小波引导的KV缓存过滤增强扩散大语言模型的长上下文能力
authors: "Jinnan Yang, Yan Wang, Zhen Bi, Kehao Wu, Xiaojie Li, Jungang Lou, Zechao Li, Jing Liu"
date: 2026-05-30
pdf: "https://arxiv.org/pdf/2606.00724v1"
tags: ["query:key-tokens"]
score: 9.0
evidence: 过滤超长上下文中的关键token
tldr: 扩散LLM因多步迭代推理在长上下文任务中计算开销大，现有KV缓存机制质量下降。受人类阅读启发，提出WaveFilter，无需训练，利用小波变换精确识别关键令牌构建稀疏缓存。实验证明，该方法显著提升主流KV缓存方法在复杂长上下文任务中的性能。贡献在于提供即插即用通用框架，解决长上下文瓶颈。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.00724v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 756, \"height\": 807, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.00724v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1597, \"height\": 778, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.00724v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1447, \"height\": 855, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.00724v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1648, \"height\": 678, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.00724v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1650, \"height\": 512, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.00724v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1558, \"height\": 291, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.00724v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1650, \"height\": 926, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.00724v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1647, \"height\": 651, \"label\": \"Table\"}]"
motivation: 扩散LLM长上下文推理中KV缓存机制生成质量严重下降，需精确高效过滤关键令牌。
method: 提出WaveFilter，引入小波变换分解长序列，精准识别关键令牌，构建稀疏KV缓存。
result: 实验表明，WaveFilter即插即用，显著提升现有KV缓存方法在复杂长上下文任务中的性能。
conclusion: WaveFilter为扩散LLM长上下文部署提供有效且无需训练的通用缓存框架。
---

## 摘要
扩散大语言模型（DLMs）在各种任务中展现出显著优势。然而，受限于其多步迭代推理机制，它们在长上下文任务中的计算开销和推理延迟已成为限制其大规模部署的核心瓶颈。在处理长序列时，现有的键值（KV）缓存机制常常面临生成质量急剧下降的困境，其核心挑战在于如何在超长上下文中精确高效地过滤关键标记。受人类阅读过程的启发，我们提出了WaveFilter，一个通用且无需训练的缓存框架。该框架创新性地引入小波变换对长序列进行分解，以实现关键标记的精确识别，并基于此构建稀疏KV缓存来计算最终的上下文表示。实验结果表明，WaveFilter作为一个即插即用的通用框架，显著增强了现有主流KV缓存方法在复杂长上下文任务中的性能。

## Abstract
Diffusion Large Language Models (DLMs) have demonstrated significant advantages across various tasks. However, constrained by their multi-step iterative inference mechanism, their computational overhead and inference latency in long-context tasks have become core bottlenecks restricting their large-scale deployment. When processing long sequences, existing Key-Value (KV) caching mechanisms often face a dilemma where generation quality degrades drastically, where the core challenge lies in precisely and efficiently filtering critical tokens within ultra-long contexts. Inspired by the human reading process, we propose \textbf{WaveFilter}, a universal and training-free caching framework. This framework innovatively introduces the wavelet transform for decomposition of long sequences to achieve precise identification of key tokens, based on which a sparse KV Cache is constructed to compute the final contextual representation. Experimental results demonstrate that WaveFilter, as a plug-and-play generic framework, significantly enhances the performance of existing mainstream KV Cache methods in complex long-context tasks.

---

## 论文详细总结（自动生成）

# 论文结构化总结：WaveFilter

## 1. 核心问题与整体含义（研究动机和背景）

- **核心问题**：扩散大语言模型（DLMs）因多步迭代推理机制，在长上下文任务中计算开销和推理延迟巨大。现有的 Key-Value (KV) 缓存机制（如 Fast-dLLM、Elastic-Cache）在超长上下文中生成质量急剧下降，核心挑战在于**如何精确且高效地过滤出关键令牌**。
- **研究动机**：受人类“先略读后精读”的认知习惯启发，作者希望设计一种**无需训练、即插即用**的框架，通过粗粒度全局感知和细粒度局部定位两阶段策略，提升现有 KV 缓存方法在长上下文任务中的性能。
- **整体含义**：提出 WaveFilter，利用**离散小波变换（DWT）**对长序列进行多尺度分解，精准识别关键令牌并构建稀疏 KV 缓存，从而在不牺牲生成质量的前提下加速推理。

## 2. 方法论：核心思想、关键技术细节

- **核心思想**：模仿人类阅读的“先构建宏观语义结构，再局部精确定位”过程，通过小波变换实现粗→细的多尺度令牌过滤。
- **总体流程**（两个阶段）：
  - **阶段1：粗粒度全局感知**——利用 DWT 提取缓存键（Key）的低频分量，在压缩空间中快速定位与当前查询（Query）高度相关的候选区域。
  - **阶段2：细粒度局部定位**——通过**多尺度递归过滤**，从候选区域中逐步缩小范围，最终选出最关键的令牌索引，构建稀疏的 Key 矩阵和 Value 矩阵。
- **关键技术细节**：
  1. **DWT 分解**：对 Prompt 部分的 Key 缓存 \( \mathbf{eK}_{t-1,l}[I_p] \) 进行 B 级小波分解，得到低频近似分量 \( \mathbf{eK}_{t-1,l}^{(B)} \)，滤除高频噪声。
  2. **重要性评估与 Top-K 选择**：计算 Query 与低频分量的注意力权重矩阵，按列求和得到重要性向量，选取比例 \( m_B \) 的最高分区域作为候选集 \( \mathcal{J}_B \)。
  3. **尺度映射与递归**：将候选区域索引映射回原始序列空间，再对新子序列进行更高分辨率的小波分解（B-1 尺度），重复注意力和选择过程，直至尺度为 1。
  4. **稀疏缓存构建**：最终得到的精确索引 \( \mu_0 \) 对应的键值和当前生成位置的键值拼接，形成稀疏缓存参与最终注意力计算。
- **公式与算法**（文字说明）：
  - 低频分量提取：\( \mathbf{eK}_{t-1,l}^{(B)} = \text{DWT}(\mathbf{eK}_{t-1,l}[I_p], \psi) \)
  - 注意力权重：\( \mathbf{A}_{t,l}^{(B)} = \text{Softmax}\left( \frac{\mathbf{Q}_{t,l} (\mathbf{eK}_{t-1,l}^{(B)})^T}{\sqrt{d}} \right) \)
  - 重要性向量：\( \mathbf{W}_{t,l}^{(B)} = \sum_i \mathbf{A}_{t,l}^{(B)}_i \)
  - Top-K 选择：\( \mathcal{J}_B = \text{Top-K}(\mathbf{W}_{t,l}^{(B)}, m_B) \)
  - 索引映射：\( \mu_{B-1} = \{ 2^B \cdot j + k \mid j \in \mathcal{J}_B, k \in \{0,\dots,2^B-1\} \} \)

## 3. 实验设计

- **数据集 / 场景**：
  - **LongBench**：包含单文档问答（Qasper, MultifieldQA_en）、多文档问答（HotpotQA, 2WikiMultihopQA, Musique）、少样本学习（TREC, TriviaQA）、合成任务（PassageCount, PassageRetrieval_en）、代码补全（LCC）。
  - **Ruler**：涵盖多种上下文长度（4K、8K）的10个子任务，如单/多针检索、多值/多查询检索、变量追踪、常见词提取、单跳/多跳问答。
- **Benchmark 与对比方法**：
  - 基准：LLaDA-8B-Instruct、Dream-v0-Base-7B。
  - 对比方法：Fast-dLLM、Elastic-Cache。
  - 增强方法：Fast-dLLM & WaveFilter、Elastic-Cache & WaveFilter。
- **评估指标**：准确率（Accuracy）、吞吐量（Tokens/sec）、加速比（相对于全注意力基线）、总运行时间（Total Runtime）。

## 4. 资源与算力

- **硬件**：单张 **NVIDIA A800 80GB GPU**。
- **训练时长**：文中未明确报告训练时长（因为 WaveFilter 是无需训练的推理加速框架，仅需推理时间）。消融实验中给出了总运行时间，如 Longbench 上 LLaDA-8B 的 Fast-dLLM & WaveFilter 总运行时间为 36.28 分钟（Qasper 子集）等，但未提供整体训练/总耗时。
- **说明**：论文未详细给出模型训练的算力消耗，重点在推理阶段的加速评估。

## 5. 实验数量与充分性

- **实验组数**：较多，包括：
  - 两大基准（LongBench、Ruler）上两个模型（LLaDA-8B、Dream-v0-Base-7B）的全面评估，覆盖约 20 个子任务。
  - 消融实验：针对关键超参数 **Scale (B)** 和 **Proportion (m_B)** 进行组合分析（共 4×4=16 组配置），从准确率、吞吐量、总运行时间三个维度展示热力图。
- **充分性与公平性**：
  - 对比方法均在相同硬件和软件环境重新运行，排除了环境差异。
  - 超参数保持一致（如置信度解码阈值 0.9、最大生成长度 256、Haar 小波基），仅比例在 [0.3,1] 内按数据集调整，体现了方法的鲁棒性。
  - 同时报告了总生成 token 数和总运行时间，避免仅依赖吞吐量的片面性。
- **结论**：实验覆盖全面，对比客观，消融分析深入，充分验证了 WaveFilter 的有效性。

## 6. 主要结论与发现

- WaveFilter 作为**通用、即插即用**的框架，能显著提升 Fast-dLLM 和 Elastic-Cache 在长上下文任务中的准确率，同时维持或提升吞吐量。
- 在 LongBench 上，例如单文档 QA 任务（Qasper），Fast-dLLM & WaveFilter 将准确率从 22.03% 提升至 28.73%，且吞吐量仍高于全注意力基线。
- 在 Ruler 上，8K 上下文下，vt 任务中 Fast-dLLM 准确率 19.08%，加 WaveFilter 后提升至 21.56%；Elastic-Cache 从 25.72% 提升至 26.04%。
- **消融结论**：
  - 中间尺度（B=2 或 B=3）达到最佳准确率；过浅（B=1）或过深（B=4）均不利。
  - 中等比例（0.3~0.6）既能保留关键语义又能滤除噪声，准确率最高；过大或过小都会降低效率或质量。
- 多尺度递归过滤是核心，能实现从宏观到微观的精准定位。

## 7. 优点

1. **无需训练**：可直接嵌入现有 KV 缓存方法，无需额外训练开销。
2. **通用即插即用**：兼容 Fast-dLLM、Elastic-Cache 等多种缓存策略，适用于不同扩散 LLM。
3. **创新方法**：首次将小波变换用于 KV 缓存过滤，利用其多分辨率特性实现高效粗→细筛选。
4. **实验设计严谨**：同时报告准确率、吞吐量、总运行时间，避免单一指标偏差；消融实验充分揭示超参数影响。
5. **显著提升长上下文性能**：在许多子任务上准确率提升 5~10 个百分点，且资源开销增加不大。

## 8. 不足与局限

1. **吞吐量指标片面性**：论文指出在某些任务中（如 Ruler 的 s1 子集），WaveFilter 导致吞吐量下降，但总运行时间更短、生成质量更高，说明 throughput 无法完全反映实际效率。
2. **额外计算开销**：虽然小波变换压缩了序列，但计算注意力权重矩阵（Equation 7）仍引入不可忽略的复杂度，导致部分任务吞吐量降低。
3. **摘要任务性能下降**：在少样本学习（如 TREC、TriviaQA）等需要全局上下文和具体指令的任务中，WaveFilter 的稀疏化可能遗漏关键提示，导致准确率下降。
4. **未覆盖更复杂的任务**：实验仅涉及问答、检索、代码补全等，未验证在对话、长文本生成等更复杂场景下的表现。
5. **超参数依赖**：比例 m_B 需针对数据集调整（0.3~1），缺乏自动自适应机制，可能增加调参成本。
6. **资源报告不完整**：未提供完整训练或推理的总 GPU 小时数，难以复现总能耗。

（完）
