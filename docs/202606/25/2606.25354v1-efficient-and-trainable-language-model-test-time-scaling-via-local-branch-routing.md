---
title: Efficient and Trainable Language Model Test-Time Scaling via Local Branch Routing
title_zh: 通过局部分支路由实现高效可训练的语言模型测试时扩展
authors: "Yutong Yin, Mingyu Jin, Jin Pan, Changyi Yang, Zijie Xia, Dhruv Pai, Shuming Hu, Zhen Zhang, Chenyang Zhao, Jinman Zhao, Wujiang Xu, Raymond Li, Xin Eric Wang, Julian McAuley, Zhaoran Wang"
date: 2026-06-24
pdf: "https://arxiv.org/pdf/2606.25354v1"
tags: ["query:key-tokens"]
score: 8.0
evidence: 基于隐状态的token级路由决策
tldr: "现有语言模型测试时扩展面临效率与可训练性的权衡。本文提出局部分支路由(LBR)，在token级扩展局部前瞻树，通过轻量级路由器选择深度1子树，实现离散分支身份保持和可导轨迹似然。结合强化学习联合优化模型与路由器，在数学推理任务上提升Pass@1和Pass@32，验证了轻量局部分支作为高效可训练离散测试时扩展的有效性。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.25354v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1725, \"height\": 752, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.25354v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1719, \"height\": 597, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.25354v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1695, \"height\": 856, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.25354v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1728, \"height\": 704, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.25354v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 819, \"height\": 489, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.25354v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 784, \"height\": 604, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.25354v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 729, \"height\": 547, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.25354v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1918, \"height\": 754, \"label\": \"Table\"}]"
motivation: 现有测试时扩展方法或为单线程长链思维，或为开销大的搜索，难以端到端训练。
method: 局部分支路由(LBR)扩展局部前瞻树，路由器基于候选分支的隐藏状态选择子树，支持离散分支和可导强化学习。
result: "在数学推理基准上，LBR提升了Pass@1和Pass@32，优于离散思维链、标准RLVR及软分支基线。"
conclusion: 轻量局部分支提供了一种高效、可训练的离散测试时扩展方案。
---

## 摘要
测试时扩展能够改善语言模型的推理能力，但现有方法常面临艰难权衡：长链思维采样仍保持单线程，而句子级或解决方案级搜索可能计算开销大且难以端到端训练。我们提出局部分支路由（LBR），一种词元级测试时扩展框架，它扩展一个小型局部前瞻树，将所有采样分支向前传递通过语言模型，并使用轻量级路由器选择深度为1的子树进行提交。通过路由候选局部未来的隐藏状态，LBR允许每个词元决策利用超越根节点下一个词元分布的证据，同时避免完整的解决方案级搜索。由此产生的剪枝-移位-增长解码过程保留了离散分支身份，并定义了可处理的树轨迹似然：新增长的节点在首次采样时被计数，路由器决策被赋予显式概率。这使得能够使用可验证奖励进行端到端强化学习，在相同的似然比原则下联合优化基模型和路由器，如同离散词元RLVR。在合成层次规划任务上，LBR显示候选后隐藏状态提供了有用的路由证据。在数学推理基准上，LBR在Pass@1和Pass@32上均优于离散思维链、普通离散词元RLVR和RL兼容的软词元分支基线。这些结果表明，轻量级局部分支为语言模型测试时扩展提供了一种高效、可训练且离散的形式。

## Abstract
Test-time scaling improves language-model reasoning, but existing approaches often face a difficult trade-off: long chain-of-thought sampling remains single-threaded, while sentence- or solution-level search can be computationally expensive and hard to train end-to-end. We introduce Local Branch Routing (LBR), a token-level test-time scaling framework that expands a small local lookahead tree, forwards all sampled branches through the language model, and uses a lightweight router to select the depth-1 subtree to commit. By routing over the hidden states of candidate local futures, LBR allows each token decision to use evidence beyond the root next-token distribution while avoiding full solution-level search. The resulting prune-shift-grow decoding process preserves discrete branch identities and defines a tractable tree-trajectory likelihood: newly grown nodes are counted when first sampled, and router decisions are assigned explicit probabilities. This enables end-to-end reinforcement learning with verifiable rewards, jointly optimizing the base model and router under the same likelihood-ratio principle as discrete-token RLVR. On synthetic hierarchical-planning tasks, LBR shows that post-candidate hidden states provide useful routing evidence. On mathematical reasoning benchmarks, LBR improves both Pass@1 and Pass@32 over discrete chain-of-thought, vanilla discrete-token RLVR, and RL-compatible soft-token branching baselines. These results suggest that lightweight local branching offers an efficient, trainable, and discrete form of language-model test-time scaling.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：语言模型在推理时扩展计算量可提升性能，但现有方法面临权衡：长链思维采样仍是单线程的，而句子级或解决方案级搜索（如树搜索）计算开销大且难以端到端训练。现有的软标记分支方法（如Soft Thinking、Multiplex Thinking）将候选标记合并为连续混合嵌入，丢失了离散分支的身份信息，且路由决策不够透明。
- **研究动机**：希望设计一种轻量级的、token级别的测试时扩展方法，既能利用局部未来信息进行决策，又能保持离散分支的可解释性和可训练性，避免全局搜索的高成本。
- **整体含义**：通过局部前瞻树（local lookahead tree）和轻量级路由器，在token级别实现“先成长（forward）后路由（route）”，使每个token的决策可基于候选后隐藏状态，同时保持离散性并定义可训练的概率轨迹，从而实现高效、可训练的离散测试时扩展。

## 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程

- **核心思想**：局部分支路由（Local Branch Routing, LBR）在解码的每一步维护一个深度为L、宽度为K的局部前瞻树（local lookahead tree）。首先成长（grow）该树——从当前前缀采样K个候选下一个标记，并继续向前采样直到深度L，同时将所有采样节点通过语言模型前向传播得到隐藏状态。然后使用轻量级路由器（router）根据所有候选子树的隐藏状态选择其中一个深度1的子树（即候选标记）进行提交，提交的标记成为已承诺前缀的一部分。未选中的子树被剪枝（prune），选中的子树向前移位（shift），并成长一个新层恢复深度L。这个过程重复进行。

- **关键技术细节**：
  - **树成长（Grow）**：从根节点开始，每个活跃节点从语言模型分布采样K个孩子（使用温度、top-p等过滤），并前向传播得到隐藏状态。
  - **路由器（Router）**：使用基于集合注意力（set attention）的网络，先独立编码每个候选子树（对于L=1，子树就是单个候选标记的隐藏状态），再通过跨子树集合注意力进行对比，输出一个softmax分布。路由器采样或选择下标k*，提交对应标记。
  - **剪枝-移位-成长（Prune-Shift-Grow）**：选择后丢弃其他子树；选中的子树根节点已提交，其子节点成为下一轮的局部树；然后成长一个新层恢复深度L。
  - **轨迹似然**：整个解码过程的似然由两部分组成：新成长节点的语言模型对数概率（首次采样时计数）和路由器决策的对数概率。公式：
    log p(F|q) = Σ_t Σ_{v∈G_t} log π̃_θ(tok(v)|ctx(v)) + Σ_t log ρ_ϕ(k*_t | x_{<t}, A_t; θ)
    其中G_t是步骤t新成长的节点集合。这个似然可用于端到端强化学习（RLVR），采用与离散token RLVR相同的似然比原则。

- **算法流程（文字描述）**：
  1. 初始化：构建第一个深度L的局部树（从初始前缀成长）。
  2. 对每一步t：
     a. 成长：从当前树的前沿节点采样并前向传播，得到新的子节点，填满深度L。
     b. 路由：路由器根据所有候选子树的隐藏状态（对于L=1，即K个候选下一个标记的隐藏状态）选择其中一个深度1的子树。
     c. 提交：提交该子树的根标记，添加到输出序列。
     d. 剪枝：丢弃其他未被选中的子树。
     e. 移位：选中的子树（除根节点外）成为下一轮的局部树。
     f. 成长（为下一次准备）：从移位后的树的前沿节点再成长一个新层，恢复深度L。
  3. 重复直到结束。

## 3. 实验设计：数据集/场景、基准与对比方法

- **合成层次规划任务（Radix-Translated Reachability）**：
  - 场景：基于ProsQA改进的图可达性任务，将节点ID转换为定长二进制串，形成token级层次分支。模型需从根节点生成到可达目标的路径。
  - 对比方法：离散CoT（标准自回归）、Soft Thinking（混合嵌入）、LBR（本文方法）。
  - 评估：目标准确率（target accuracy）、路由器准确率、LM KL散度、探针分析（probe）。

- **数学推理基准**：
  - 数据集：Minerva, AIME’25, AIME’24, MATH500, AMC’23, Olympiad（共6个）。
  - 模型基座：DeepSeek-R1-Distill-Qwen-1.5B 和 7B。
  - 对比方法：
    - 离散CoT（无RL训练）
    - 标准离散token RLVR（GRPO）
    - RL兼容的软token分支：Multiplex Thinking（采样候选并混合嵌入）
    - LBR（本文方法，L=1, K=3；以及L=2, K=3）
  - 评估指标：Pass@1 和 Pass@32（平均64次采样）。

- **消融实验**：
  - 路由器消融：对比完整集合注意力路由器与独立路由器（移除跨子树注意力，每个候选独立评分）。
  - 局部前瞻深度消融：L=1 vs L=2。
  - 训练动态分析：观察Pass@1、Pass@32、路由器熵、基模型熵的变化。

## 4. 资源与算力

- 文中明确提到：所有训练使用8×H100 GPUs。
- 训练设置：训练300步，全局batch size为128个问题，每个问题采样8个rollout。
- 训练温度1.0，top-p=1.0；评估时top-p=0.95。
- 未明确说明具体训练时长（小时数），但提及“All the trainings are done in 8×H100 GPUs”，且训练步数较少（300步），因此算力需求相对适中。

## 5. 实验数量与充分性

- **实验组数**：至少包括：
  - 合成任务：3种方法对比 + 探针分析。
  - 数学推理：3种基线 + 2种LBR配置 + 2种模型规模 = 共约5×2=10个主要结果（每个数据集报告Pass@1和Pass@32）。
  - 消融实验：路由器消融（不同路由器类型）、深度消融（L=1 vs L=2）。
  - 训练动态：一个图展示熵和准确率变化。
- **充分性**：实验覆盖了合成任务和真实数学基准，对比了当前主流的离散和软标记方法，并进行了消融验证。但主要关注Pass@1和Pass@32，缺乏对其他指标（如推理时间、计算开销）的量化比较。另外，训练仅在两个较小模型（1.5B和7B）上进行，未在更大模型上验证。总体而言，实验设计是充分和客观的，但存在一定局限性。

## 6. 论文的主要结论与发现

- 在合成层次规划任务中，LBR准确率（0.967）显著高于离散CoT（0.914）和Soft Thinking（0.845）。探针分析表明：后候选隐藏状态包含更多关于最终目标的信息，而软标记混合模糊了概念身份。
- 在数学推理基准上，LBR（L=1, K=3）在Pass@1和Pass@32上均优于所有基线，包括离散CoT、离散RLVR和软标记分支。
- 局部前瞻深度从L=1增加到L=2通常进一步提升性能（尤其在7B模型上），说明更深的局部扩展提供更多有用路由证据。
- 跨子树集合注意力比独立评分更有效，说明路由是一个对比性决策。
- 训练动态显示：LBR在早期快速提升性能，路由器熵和基模型熵同时下降，表明路由器和基模型共同适应，学习更精准的局部分支选择。

## 7. 优点：方法或实验设计上的亮点

- **方法优点**：
  - 保持离散性：LBR始终在离散token空间操作，不混合嵌入，保留分支身份，提供更好的可解释性。
  - 可训练性：定义了完整的树轨迹似然，支持端到端RL训练，无需外部评估器或手工搜索控制。
  - 效率：局部前瞻树规模小（K=3, L=1或2），路由器轻量，计算开销可控，避免全局搜索。
  - 新颖性：将测试时扩展从句子级搜索降为token级局部路由，利用后候选隐藏状态，填补了现有方法的空白。
- **实验设计亮点**：
  - 合成任务精心设计（radix translation），将概念级规划转化为token级局部决策，便于分析路由机制。
  - 使用线性探针分析隐藏状态信息量，直观展示了LBR的优势来源。
  - 消融实验对比了不同路由器设计和前瞻深度，验证了关键组件的重要性。

## 8. 不足与局限

- **实验覆盖**：仅测试了1.5B和7B的小模型，未在更大模型（如70B或更大）上验证可扩展性。算力使用报告不够详细（无具体时间）。
- **评估指标**：主要报告Pass@1和Pass@32，缺少推理延迟、前向计算量、路由开销等效率指标的量化对比。
- **偏差风险**：基线方法（特别是软分支）可能未达到其最佳配置（例如超参数未详调），对比是否绝对公平存在不确定性。
- **应用限制**：
  - 局部性：LBR仅利用局部未来信息，无法进行全局搜索或长程规划，复杂问题可能受限。
  - 扩展性：深度L增大时，转发成本指数增长（树节点数KL-1），L=2时成本已较高，实际中L一般较小。
  - 路由器依赖模型隐藏状态，训练需要同时优化路由器与基模型，可能引入更多超参数和收敛挑战。
- **理论分析**：虽然提供了启发式解释，但缺少严格的理论保证（例如为什么局部路由优于软分支）。文中提到“constructive expressivity results”仅作为相关工作进行引用，未对LBR进行类似分析。

（完）
