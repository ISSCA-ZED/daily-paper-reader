---
title: Dynamic Linear Attention
title_zh: 动态线性注意力
authors: "Xin Wang, Hui Shen, Boyuan Zheng, Xueshen Liu, Minkyoung Cho, Zhongwei Wan, Zesen Zhao, Zhuoqing Mao, Shen Yan, Mi Zhang"
date: 2026-06-09
pdf: "https://arxiv.org/pdf/2606.10650v1"
tags: ["query:key-tokens"]
score: 8.0
evidence: 线性注意力中动态变化的token重要性
tldr: 标准注意力二次复杂度限制大模型长上下文扩展，线性注意力虽降低复杂度，但多状态方法依赖固定合并策略，无法适应token重要性动态变化，导致严重误差累积。本文提出DLA动态记忆建模框架，包含信息感知动态状态合并和容量受限记忆建模，自适应划分边界并控制内存增长。在两种线性注意力模型上预训练，16个数据集评测显示全面超越现有方法。该工作有效解决了线性注意力长序列信息丢失问题，提升了表示能力。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.10650v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1716, \"height\": 310, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.10650v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1701, \"height\": 509, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.10650v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1124, \"height\": 425, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.10650v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1721, \"height\": 355, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.10650v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1384, \"height\": 290, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.10650v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1042, \"height\": 263, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.10650v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1041, \"height\": 192, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.10650v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 840, \"height\": 274, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.10650v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 840, \"height\": 328, \"label\": \"Table\"}]"
motivation: 标准注意力二次复杂度限制长上下文，现有线性注意力多状态方法固定合并策略导致重要token信息丢失和错误累积。
method: 提出DLA框架，包含信息感知动态状态合并和容量受限记忆建模，自适应调整状态边界并控制内存增长。
result: 在两种线性注意力模型上预训练，16个数据集上超越现有方法，验证了动态记忆建模的有效性。
conclusion: DLA通过动态合并策略显著提升线性注意力长上下文表示能力，为大规模语言模型处理长序列提供有效方案。
---

## 摘要
大型语言模型（LLMs）在处理长上下文时的可扩展性从根本上受限于标准注意力的二次复杂度，这促使人们采用具有次二次成本的线性注意力机制。为了提升长上下文下的表示能力，近期的方法以多状态方式组织记忆。然而，现有的多状态线性注意力方法依赖于固定的状态合并策略，无法适应动态变化的词元重要性，从而不可逆地模糊关键词元，并在长序列中导致严重的误差累积。为解决这一局限，我们提出了 DLA，一种多状态线性注意力的动态记忆建模框架。DLA 引入了 (i) 信息感知动态状态合并，该方法基于词元级别的信息变化自适应地确定状态边界，在语义转换周围保留高分辨率表示，同时积极总结稳定区域；(ii) 容量受限记忆建模，通过选择性地合并相邻的低信息状态来维护一个固定大小、按时间顺序排列的状态缓存，以最小化信息损失控制记忆增长。我们在两种不同的线性注意力模型上预训练了 DLA，并在三个类别的 16 个数据集上进行了评估。实验结果表明 DLA 优于现有最先进方法。

## Abstract
The scalability of Large Language Models (LLMs) to long contexts is fundamentally constrained by the quadratic complexity of standard attention, motivating the adoption of linear attention mechanisms with sub-quadratic cost. To improve representation capacity under long contexts, recent approaches organize memory in a multi-state manner. However, existing multi-state linear attention methods rely on fixed state merging policies that cannot adapt to dynamically varying token importance, irreversibly obscuring critical tokens and causing severe error accumulation over long sequences. To address this limitation, we propose DLA, a dynamic memory modeling framework for multi-state linear attention. DLA introduces (i) Information-Aware Dynamic State Merging, which adaptively determines state boundaries based on token-level information variation, preserving high-resolution representations around semantic transitions while aggressively summarizing stable regions, and (ii) Capacity-Bounded Memory Modeling, which maintains a fixed-size, chronologically ordered state cache by selectively merging adjacent low-information states to control memory growth with minimal information loss. We pre-train DLA on two different linear attention models and evaluate on 16 datasets across three categories. Experimental results demonstrate the superiority of DLA over state-of-the-art.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：标准Transformer的自注意力机制具有二次计算和内存复杂度（相对于序列长度T），严重限制了大型语言模型（LLMs）在长上下文场景下的可扩展性。
- **研究动机**：为克服二次复杂度，线性注意力（linear attention）被提出，其通过重新排列计算顺序实现O(T)复杂度。近期，多状态线性注意力方法（如Log-Linear Attention）通过将历史上下文分割为多个块并压缩为独立状态，进一步提升了表示能力。
- **现有方法局限**：这些方法依赖固定的块大小或规则性的合并策略（如固定时间调度），无法适应词元重要性的动态变化。关键语义转换处的词元可能被过早吸收到粗略总结中，且合并决策不可逆，导致长序列下误差累积。

### 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：提出**动态线性注意力（DLA）**框架，用信息感知的动态状态合并替代固定策略，并引入容量受限的记忆建模，从而在保持效率的同时提升长上下文表示质量。
- **关键技术细节**：
  - **信息感知动态状态合并**：
    - 定义**状态信息分数** \( I_t = \frac{\|S_t - S_{t-1}\|_F}{\|S_{t-1}\|_F + \epsilon} \)，其中 \( S_t \) 是当前词元的状态表示（由键值对构成），\( S_{t-1} \) 是上一个记忆状态。分数衡量新词元相对于当前状态的表示变化。
    - 设定阈值 \( \tau \)，当 \( I_t \ge \tau \) 时，当前词元发起新的记忆状态；否则合并到现有状态。预训练时使用软门控，推理时使用硬分割。
    - 理论证明（定理3.1、推论3.2）表明，固定阻塞策略在非平稳序列上导致更大偏差，而DLA贪心地最小化块内异质性项。
  - **容量受限记忆建模**：
    - 维护一个固定大小K的、按时间顺序排列的状态缓存 \( M = \{(S_i, n_i, \bar{I}_i)\} \)，其中 \( n_i \) 是该状态包含的词元数，\( \bar{I}_i \) 是聚合信息分数。
    - 当缓存满时（m=K），选择相邻信息密度 \( (\bar{I}_i + \bar{I}_{i+1})/(n_i + n_{i+1}) \) 最低的一对状态合并，释放一个槽位。
    - 最终输出通过线性注意力加权求和得到：\( o_t = \sum_{i=1}^m \lambda_{t,i} q_t^\top S_i \)，其中 \( \lambda_{t,i} \) 由预训练学习得到。
- **算法流程**：先执行信息感知动态合并（算法1），再执行容量受限记忆建模（算法2），两者结合形成DLA。

### 3. 实验设计

- **数据集**：涵盖三大类共16个数据集：
  - **常识推理**（8个）：LAMBADA、PIQA、HellaSwag、WinoGrande、ARC-e、ARC-c、OpenBookQA、CommonsenseQA。
  - **上下文检索**（6个）：SWDE、SQuAD、FDA、TriviaQA、Drop、NQ。
  - **长上下文建模**（2个）：RULER（含单针和多针任务）、LongBench（含多文档QA、摘要、少样本学习等）。
- **基准模型**：
  - 裸线性注意力：Mamba-2-780M、Gated DeltaNet-1.3B。
  - 多状态线性注意力：上述两个模型的Log-Linear Attention变体。
  - 完整注意力Transformer（约778M参数）作为参考。
- **评估框架**：使用LM-Evaluation-Harness框架，所有模型在相同设置下比较。

### 4. 资源与算力

- **算力**：使用4张NVIDIA A100 GPU进行预训练和推理实验。预训练在Long-Data-Collections数据集上进行，序列长度16K，总计50B tokens。训练时长未明确说明。
- **其他**：DLA将状态缓存容量K设为30（与Log-Linear最大状态数一致），保证公平对比。

### 5. 实验数量与充分性

- **实验数量**：共进行了跨3类16个数据集的全面评估，包括：
  - 常识推理（8个数据集）
  - 上下文检索（6个数据集）
  - 长上下文（RULER多长度、LongBench 12个子任务）
  - 消融实验：模块敏感性（DLA(I) vs DLA）、超参数敏感性（容量K=20/30/40，阈值τ=0.5/0.6/0.7）
  - 效率实验：不同batch size和上下文长度下的吞吐量和显存对比
- **充分性与客观性**：
  - 覆盖多种任务类型（推理、检索、长文档理解），提高泛化说服力。
  - 对比了两种主流线性注意力骨干，且与full-attention Transformer作为上界对比。
  - 消融实验设计合理，验证了每个组件的贡献。
  - 效率实验考虑了实际部署场景（不同batch size和序列长度）。
  - 实验规模属于中等偏上（50B tokens预训练），但未在更大模型（如7B）上验证，结论的泛化性可能受限于780M-1.3B规模。

### 6. 论文的主要结论与发现

1. **DLA在16个数据集上一致优于所有基线**，包括裸线性注意力和Log-Linear变体，在Mamba-2和Gated DeltaNet上均有显著提升。
2. **DLA在部分任务上甚至达到或超过完整注意力Transformer**：例如Mamba-2+DLA在常识推理平均准确率34.2%高于Transformer的32.9%。
3. **效率优势**：相较于Log-Linear Attention，DLA在相同状态容量下实现更高吞吐量和更低运行时内存。
4. **动态合并是关键**：信息感知合并比单纯容量限制贡献更大，且两种组件协同效果更好。

### 7. 优点

- **方法创新性**：首次提出基于词元信息变化动态决定合并边界的线性注意力机制，适应非均匀语义结构。
- **理论支撑**：提供了状态偏差上界（定理3.1）和固定政策次优性（推论3.2）的数学证明，增强说服力。
- **实用性与效率兼顾**：容量受限设计保证推理成本可控，且实验显示对容量和阈值超参数鲁棒。
- **实验全面性**：覆盖推理、检索、长文档三大类，并进行消融和效率分析，结果可靠。
- **代码复现基础**：遵循Log-Linear Attention的开源设置，便于验证。

### 8. 不足与局限

- **模型规模局限**：仅在780M和1.3B参数模型上验证，未在更大规模（如7B、70B）上测试，实际应用中的可扩展性未知。
- **预训练数据与周期**：使用50B tokens的学术规模数据（Long-Data-Collections），非工业级数万亿tokens，与前沿LLM预训练存在差距。
- **长上下文任务有限**：RULER和LongBench最长序列长度未明确超过16K测试（但预训练用16K），更极长上下文（如100K+）可能暴露问题。
- **动态合并阈值τ的固定性**：虽然实验显示鲁棒，但最佳τ可能随模型层数、数据分布变化，需要手工调整，未提供自适应方法。
- **与SSM家族对比不足**：未与同规模的其他高效架构（如Mamba-1、RWKV、RetNet）对比，仅与Mamba-2和Gated DeltaNet变体比较。
- **位置编码与时间顺序**：虽保持了时间顺序，但未讨论位置编码的显式整合，可能影响长距离依赖建模精度。
- **效率实验中只测量了预填充阶段**，解码阶段的端到端延时未见分析。

（完）
