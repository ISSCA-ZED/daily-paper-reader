---
title: "Legible-by-Construction: Attention and End-to-End Transformers"
title_zh: 可构建的可读性：注意力机制与端到端Transformer
authors: Mark Oskin
date: 2026-07-05
pdf: "https://arxiv.org/pdf/2607.04319v1"
tags: ["query:key-tokens"]
score: 8.0
evidence: 通过sigmoid门控注意力值实现关键token的隐藏状态分析
tldr: "Transformer注意力机制不透明，本文通过sigmoid约束值通道使其成为可读特征检测器，并设计Boolean变体引入显式集合操作。在125M参数模型中，44%-62%的通道成为清晰、上下文选择性的检测器，可读性随深度提升，语言质量与基线相当。最终将可读注意力与可读前馈层结合，构建了端到端可解释的语言模型，能读取生成token的组成单元。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.04319v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1624, \"height\": 823, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.04319v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1698, \"height\": 652, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.04319v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1044, \"height\": 684, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.04319v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1493, \"height\": 850, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.04319v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1659, \"height\": 821, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.04319v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1580, \"height\": 613, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.04319v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1466, \"height\": 344, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.04319v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1427, \"height\": 795, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.04319v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1308, \"height\": 851, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.04319v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 994, \"height\": 733, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.04319v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1359, \"height\": 439, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.04319v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1433, \"height\": 341, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.04319v1/table-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1499, \"height\": 297, \"label\": \"Table\"}]"
motivation: 前馈层已被改造为可读逻辑算子，但注意力层仍不透明，需要设计可读注意力机制以构建完全可解释的Transformer。
method: 对注意力头值施加sigmoid激活使每个通道成为可读检测器，并引入Boolean变体显式建模交集与差集；输出投影保持自由以增强选择性。
result: "五种注意力设计中44%-62%的值通道成为清晰选择性检测器，可读性随深度增加，语言模型质量与标准基线持平。"
conclusion: 通过耦合可读注意力和前馈层，实现了端到端可解释的语言模型，其内部单元均为命名集合与量词操作，可直接读取生成过程。
---

## 摘要
一篇配套论文表明，Transformer的前馈层可以通过显式的模糊集运算（交集、集合差和自遗忘序列量词）重建，从而其隐藏单元可作为命名的逻辑运算符解读，且不影响语言模型质量。这使得Transformer的另一半（注意力机制）仍不透明。本文将该思想引入注意力机制，并将两者融合为一个模型。其机制极简：通过sigmoid传递注意力头部的值，使得每个值通道成为特征是否在令牌上存在的可读检测器。这未增加任何参数，且在其余部分保持标准头部不变。一种布尔变体进一步将值重组为显式的令牌内交集和可否定集合差。两种设计中，输出投影保持自由，未绑定到词汇表，这是关键决策：限制头部检测的内容，同时不约束其写入的内容，可产生选择性检测器；而约束写入则不然。通过两种选择性压力（稀疏触发和极端触发），有界值被塑造成可读检测器，不同设计所需的压力并非通用。在125M参数的五个专用注意力设计中，44%至62%的值通道成为清晰、上下文相关的选择性检测器，且其可读性随深度增加而提升，而不仅局限于标点符号上形成。语言模型质量与常规基线持平。最后，我们将布尔注意力与可读前馈层耦合，训练了一个端到端可构建可读性的语言模型，达到基准性能：其前馈单元始终是命名的集合和量词操作，我们可以取用其生成的令牌，并读取组成产生该令牌的命名单元。

## Abstract
A companion paper showed that a transformer's feed-forward layer can be rebuilt from explicit fuzzy set operations - intersection, set-difference, and a self-forgetting sequence quantifier - so its hidden units read as named logical operators at no cost to language-model quality. That left the other half of the transformer opaque. Here we carry the same idea into attention and join the two into one model. The mechanism is minimal: a head's value is passed through a sigmoid, so each value channel becomes a readable detector of whether a feature holds at a token. This adds no parameters and leaves the standard head otherwise untouched. A Boolean variant goes further, restructuring the value into an explicit within-token intersection and negation-capable set-difference. In both designs the output projection is left free, not tied to the vocabulary, which is the load-bearing decision: bounding what a head detects while leaving what it writes unconstrained yields selective detectors, whereas constraining the write does not. A bounded value is shaped into a readable detector by two selectivity pressures - one for sparse firing, one for decisive firing at the rails - and which a design wants is not universal. Across five specialized-attention designs at 125M parameters, 44 to 62 percent of value channels become crisp, contextually selective detectors, and their legibility rises with depth rather than crystallizing only on punctuation. Language-model quality is at parity with a conventional baseline. Finally, we couple the Boolean attention to the legible feed-forward layer and train an end-to-end legible-by-construction language model at benchmark parity: its feed-forward units are named set and quantifier operations throughout, and we can take a token it generates and read the named units that compose to produce it.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **背景**：Transformer的前馈层已被改造为显式模糊集运算，使隐藏单元成为命名的逻辑算子，但注意力机制仍不透明。现有可解释性工作多为事后分析（如探针、稀疏自编码器、对数透镜），这些方法依赖于外部工具，存在部分性、争议性和易过时性。
- **核心问题**：如何使注意力机制的“值”（value）部分本身成为可读的检测器，从而构建一个端到端、内在可解释的语言模型，无需事后重构。
- **整体意义**：通过架构设计（by construction）而不是事后分析实现可读性，使模型内部计算可以被直接读取、编辑和审计，改变语言模型作为研究对象的性质。

## 2. 论文提出的方法论：核心思想、关键技术细节

- **核心原则**：约束注意力头检测的内容（value），而自由保留其写入的内容（output projection）。绑定写入到词汇表会导致恒值坍缩。
- **两种注意力变体**：
  - **成员度头部**：在标准值投影后添加sigmoid，使每个值通道成为[0,1]的模糊隶属度，表示“该特征是否存在于当前令牌”。参数中立。
  - **布尔头部**：将单个值投影替换为两个半宽投影，产生A和B两组有界隶属度，然后拼接交集（A⊙B）和集合差（A⊙(1-B)），使值变为显式逻辑运算。同样参数中立。
- **选择性压力**：施加两种辅助损失：
  - **稀疏性压力**（λs∑|v|）：推动值向0，使通道默认关闭，仅在需要时激活。
  - **清晰度压力**（λc∑v(1-v)）：推动值向0或1的极端值，避免模糊中间值。
  - 压力必须作用于操作数，而非组合输出；不同算子所需压力组合不同（单隶属度需要两者，布尔算子只需清晰度）。
- **端到端可读模型**：将布尔注意力（100%）与可读前馈层耦合，前馈层算子比例设为25%或50%。

## 3. 实验设计：数据集、基准、对比方法

- **模型规模**：125M参数，12层，768维，可学习位置嵌入。
- **训练数据**：开放网页语料，与基线相同分词器和优化器，训练一个epoch。
- **评估基准**：
  - LAMBADA（词预测精度和困惑度）
  - BLiMP（语言可接受性）
  - ARC-Easy（常识推理）
  - WinoGrande（所有模型均为随机水平，已省略）
- **对比方法**：常规GELU基线（相同架构但无值变换和前馈算子）。
- **实验变量**：
  - 注意力设计：成员度头部、布尔头部，各占50%/75%/100%比例。
  - 选择性压力：稀疏性单独、清晰度单独、两者组合。
  - 端到端模型：注意力为100%布尔，前馈算子比例25%或50%。
  - 训练压力施加位置：仅注意力、注意力和前馈层同时施加。

## 4. 资源与算力

- **未明确说明**：文中未提及GPU型号、数量、训练时长、功耗等具体算力资源，仅提及模型为125M参数、单epoch训练。无法衡量计算成本。

## 5. 实验数量与充分性

- **实验组数**：至少包括：
  - 成员度头部（2种比例 × 3种压力 = 6组）
  - 布尔头部（3种比例 × 3种压力 = 9组）
  - 端到端模型（2种算子比例 × 2种压力位置 × 3种压力 = 12组）
  - 加上基线，共计约28组主要实验。
  - 每种压力组合还包括不同运行状态（如发散/成功）的跟踪分析。
- **全面性**：覆盖了不同注意力变体、不同混合比例、不同压力组合，并进行了端到端耦合实验。对可读性进行了量化度量（SELECTIVE/ CONSTANT/ FUZZY），并给出了代表性案例解读。
- **局限性**：所有实验均基于单个随机种子，未进行多次重复；仅在一个尺度（125M）上验证，未测试更大模型或更长时间训练；未进行统计显著性检验；部分实验（如75%成员度头部）发散但并未讨论如何修复；仅一个epoch，baseline在第二epoch表现更好，但文中指出可能为单种子噪声。

## 6. 论文的主要结论与发现

- **主要结论**：通过约束注意力头的值并将输出投影自由，可使注意力值通道成为清晰、上下文选择性的可读检测器；与常规基线相比语言质量无损失（保持同水平）。
- **具体发现**：
  - **可读性随深度增加**：深层（L7-L11）可达70-95% SELECTIVE，而浅层多为模糊或常量，反驳了“注意力头只在标点上结晶”的传统认知。
  - **压力选择取决于算子**：单隶属度值需要两个压力；布尔算子（交集）由于自身稀疏性，只需清晰度压力，增加稀疏性会导致算子饿死（ON率骤降至0.2-0.9%）。
  - **端到端模型**：耦合布尔注意力与可读前馈层后，前馈算子清晰且活跃（清晰度单独下ON率17-33%），可读性良好；注意力可读性主要在深层，浅层趋于常量。质量在基准带内。
  - **训练稳定性**：75%成员度头部会发散（因小部分无界头部超负荷），而100%或50%稳定；布尔头部无此问题。
  - **读取生成过程**：以“It was the best of times, it was the”预测“worst”为例，可分解出超格算子、否定算子等命名单元，实现直接读取。

## 7. 优点：方法或实验设计上的亮点

- **架构设计简洁**：成员度头部仅添加一个sigmoid，参数中立；布尔头部也保持参数总数不变。改动极小却能带来可读性巨大提升。
- **输出投影保持自由**：避免了绑定词汇表导致的恒值坍缩，是核心洞察。
- **双压力机制**：稀疏性和清晰度分别解决“是否触发”和“触发是否明确”两个问题，并通过分析揭示算子类型决定所需压力，提供设计指导。
- **量化可读性度量**：提出SELECTIVE、CONSTANT、FUZZY三分类，结合操作数清晰度和ON率，使可读性可衡量可比较。
- **端到端展示**：不仅展示注意力或前馈层单独可读，还构建了完整可读模型，并给出直接读取生成过程的案例，具有说服力。
- **透明揭示“意外”组成**：如超格算子竟是两个限定词检测器的集合差，这种事后不可见的结构被直接暴露，体现了架构设计可读性的优势。

## 8. 不足与局限

- **实验覆盖有限**：
  - 仅125M单尺度，未验证是否能在更大模型（1B+）上扩展。
  - 单种子，未进行多次重复以评估稳定性。
  - 仅训练一个epoch，而基线在第二个epoch表现更好，虽解释为噪声，但需更大规模验证。
  - 缺乏对训练发散情况的系统性解决方案（75%成员度发散，仅指出原因未提供修复）。
- **注意力可读性的瓶颈**：
  - 端到端模型中浅层注意力坍缩为常量，可读性主要在前馈层和深层注意力，未实现完全透明。
  - 布尔注意力仅限于令牌内组合，无法处理跨位置的关系绑定，缺乏这一重要机制。
- **可读性局限**：
  - 约一半的SELECTIVE通道仍为多义现象（polysemantic），需要进一步分析操作数才能明确含义。
  - 命名操作数的含义仍需人工解读，并非完全自动。
- **实践门槛**：训练稳定性存在非单调边界（如75%成员度发散），需要仔细调节比例，增加了使用难度。
- **缺乏软件/工具支持**：论文未提供开源代码或预训练模型，结果难以复现。

（完）
