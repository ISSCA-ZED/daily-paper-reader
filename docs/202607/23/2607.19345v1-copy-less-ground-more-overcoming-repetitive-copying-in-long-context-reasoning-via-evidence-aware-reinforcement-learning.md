---
title: "Copy Less, Ground More: Overcoming Repetitive Copying in Long-Context Reasoning via Evidence-Aware Reinforcement Learning"
title_zh: 少复制，多扎根：通过证据感知强化学习克服长上下文推理中的重复复制
authors: "Lizhe Fang, Weizhou Shen, Tianyi Tang, Yisen Wang"
date: 2026-07-21
pdf: "https://arxiv.org/pdf/2607.19345v1"
tags: ["query:key-tokens"]
score: 8.0
evidence: 区分推理轨迹中的关键证据与无关干扰以减少重复复制
tldr: 长上下文推理中大语言模型存在严重的“重复复制”问题，即模型从输入中大量复制文本而非聚焦关键证据，且随上下文长度加剧。本文提出GEAR（基于证据感知的奖励整形），在标准准确率奖励基础上增加对关键证据的重叠奖励和对无关干扰的惩罚，并构建自动标注证据的训练数据。在多个模型和基准上，GEAR平均提升4.6个点，长上下文下增益更大，同时减少重复复制和思考长度。结果表明，即使在复杂推理中，准确扎根于相关证据仍是重要且可提升的能力。
source: arxiv
selection_source: fresh_fetch
motivation: 长上下文推理中模型会从输入大量复制文本而非聚焦关键证据，导致推理失败，需针对性地通过奖励设计引导模型关注证据。
method: 提出GEAR方法，在强化学习中引入证据感知奖励：对关键证据重叠给予正奖赏，对干扰内容重叠施加惩罚，并自动生成证据标注数据以扩展至自然语言。
result: 在多种模型和基准上，GEAR相比标准RL提升最高+4.6平均分，长上下文下增益更大，同时降低重复复制和推理长度。
conclusion: GEAR证明强化学习中注入证据感知可有效缓解重复复制，提升长上下文推理质量，且该方法可自动化扩展至任意文档。
---

## 摘要
能够生成逐步推理轨迹的大语言模型在复杂任务上表现出色，将其扩展到长上下文设置已成为一个重要前沿。然而，我们在此类设置中发现了一个关键失败模式：**重复复制**，即模型在推理轨迹中大量复制输入文本，而非高效解决问题。我们表明，这种行为在前沿长上下文大语言模型中普遍存在，并随上下文长度加剧。通过将每个提示分为任务相关的关键证据和不相关的干扰上下文，我们进一步揭示了根本原因在于扎根不足：模型不加区分地复制提示内容，而未能聚焦关键证据的模型更可能给出错误答案。基于这一诊断，我们提出了GEAR（证据感知扎根奖励），一种奖励塑造方法，通过针对关键证据重叠的扎根奖励和针对不相关上下文重叠的干扰惩罚来增强准确性信号。为了在自然语言数据上实现GEAR，我们开发了一个自动化流水线，从任意文档构建带证据标注的训练数据。我们在多个模型规模和基准上验证了GEAR，显示相较于基于准确率奖励的标准强化学习，平均提升高达4.6个点，在更长上下文下收益更大，同时减少了重复复制和思考长度。我们的发现表明，即使长上下文评估从简单检索转向复杂推理，准确扎根于相关证据仍然是一项不可或缺且仍有大幅提升空间的能力。

## Abstract
Large language models that generate step-by-step reasoning traces have achieved strong performance on complex tasks, and extending them to long-context settings has emerged as an important frontier. However, we identify a critical failure mode in this regime: \emph{repetitive copying}, where models extensively copy text from the input into their reasoning traces rather than productively solving the problem. We show that this behavior is pervasive across frontier long-context LLMs and intensifies with context length. By separating each prompt into task-relevant key evidence and irrelevant distractor context, we further show that the root cause is insufficient grounding: models copy from the prompt indiscriminately, and those that fail to focus on key evidence are far more likely to answer incorrectly. Motivated by this diagnosis, we propose GEAR (Grounding Evidence-Aware Reward), a reward shaping method that augments the accuracy signal with a grounding reward for overlap with key evidence and a distractor penalty for overlap with irrelevant context. To enable GEAR on natural-language data, we develop an automated pipeline that constructs evidence-annotated training data from arbitrary documents. We validate GEAR across multiple model scales and benchmarks, showing consistent improvements of up to +4.6 average points over standard RL with accuracy-based rewards, with larger gains at longer contexts, while also reducing repetitive copying and thinking length. Our findings suggest that, even as long-context evaluation shifts from simple retrieval toward complex reasoning, accurate grounding in relevant evidence remains an indispensable capability with substantial room for improvement.

---

## 论文详细总结（自动生成）

好的，以下是对该论文《Copy Less, Ground More: Overcoming Repetitive Copying in Long-Context Reasoning via Evidence-Aware Reinforcement Learning》的结构化、深入及客观的总结。

### 1. 核心问题与整体含义 (研究动机与背景)

- **核心问题**：论文识别并深入研究了长上下文推理场景中大语言模型的一个关键失败模式：**重复复制**。即模型在处理长输入时，会将大量输入文本原封不动地复制到其内部推理轨迹中，而非进行有效的逻辑推导。
- **背景与动机**：现有具备思考能力的大语言模型在复杂任务上表现优异，并被自然地应用于长上下文推理。然而，论文发现，随着上下文长度的增加，模型的“重复复制”行为会愈发严重，这不仅导致推理效率低下（生成长度激增），还显著降低任务准确率。作者认为，该行为并非偶然，而是模型**扎根不足**的体现，即模型无法区分任务相关的关键证据和无关的干扰信息，从而进行不加区分的复制。
- **整体含义**：该研究指出，在长上下文推理中，模型从“简单检索”向“复杂推理”的能力转变固然重要，但**准确识别并聚焦于相关证据**的基本能力仍是不可或缺且亟待提升的。这为改进长上下文大语言模型的推理能力提供了新的诊断角度和解决方案。

### 2. 方法论：核心思想、关键技术细节

- **核心思想**：在强化学习的奖励塑造中嵌入证据感知，从而引导模型在推理时选择性聚焦于关键证据，并抑制对无关干扰内容的复制。
- **关键技术细节**：提出了名为 **GEAR (Grounding Evidence-Aware Reward)** 的方法，具体如下：
    1.  **奖励设计**：在标准二值准确率奖励基础上，增加两个互补的奖励信号：
        - **扎根奖励 (R_ground)**：鼓励模型的推理轨迹与**关键证据**部分（支持答案的文本）产生n-gram重叠。重叠率越高，奖励越大。
        - **干扰惩罚 (R_dist)**：惩罚模型的推理轨迹与**无关干扰**部分的n-gram重叠。重叠率越高，惩罚越重。
        - **最终奖励公式**：
            R(x, y) = R_acc + α · Overlapₙ(y∥x_key) − β · Overlapₙ(y∥x_dist)
            - `R_acc` 为二进制准确率奖励 (0/1)。
            - `x_key` 和 `x_dist` 分别代表关键证据和干扰上下文。
            - `α`, `β` 为权重系数。
            - `Overlapₙ` 为n-gram重叠率。
    2.  **自动数据构建流水线**：为将GEAR扩展到自然语言数据，论文设计了一个自动化三步骤流程，从任意文档中生成带证据标注的问答对：
        - **步骤一**：文档分析与过滤，确定文档是否适合生成问答。
        - **步骤二**：证据约束的问答生成。将文档切块，随机选取1-3个文本块作为“关键证据”，并由大语言模型基于此生成一个问题、1-3个精确引证和答案。其余文本块自动成为“干扰上下文”。
        - **步骤三**：答案验证。用独立模型基于关键证据重新回答问题，只有重生成的答案与标准答案一致时才保留数据，确保数据的准确性。

### 3. 实验设计：数据集、基准与对比方法

- **训练数据**：一个混合数据集，总计3,200个长上下文问题。
    - **结构化合成数据**：2,000个问题，来自GSM-Infinite（算术推理）和PhantomWiki（多跳实体追踪）两个程序化生成基准。
    - **自然语言数据**：1,200个问题，通过上述自动流水线从RedPajama-v2文档生成。
- **评测基准**：五个与训练数据不重叠的基准任务。
    - **Ruler**: 合成检索基准。
    - **LongBench-v2**: 综合长上下文理解基准。
    - **BrowseComp-LC**: 网页浏览理解基准。
    - **GraphWalks**: 图结构推理基准。
    - **AA-LCR**: 长上下文推理基准。
    所有基准原生使用128k令牌上下文。
- **对比方法**：
    - **Base**: 未经微调的Qwen3.5系列模型。
    - **GSPO**: 使用标准准确率作为唯一奖励的强化学习。
    - **GSPO + R_ground**: 仅添加扎根奖励，不加干扰惩罚。
    - **GSPO + GEAR (R_ground + R_dist)**: 论文提出的完整方法。
- **评价指标**：任务准确率、n-gram重叠率、思考长度。

### 4. 资源与算力

- **论文未明确提及训练所使用的具体GPU型号、数量及总训练时长**。
- 但提供了训练相关的详细配置：使用GSPO算法，选择Qwen3.5-9B, 27B, 35B-A3B三个规模的基础模型，训练1个epoch（约100个优化器步），学习率为2e-6，批次大小为32，rollout组大小为8，最大提示长度为32k，最大生成长度为16k。

### 5. 实验数量与充分性

- **实验数量多**：在3个模型规模、5个基准任务、2个上下文长度（32k和128k）上进行了评测，总计产生了大量对比数据。
- **消融实验充分**：
    - 通过对比“GSPO + R_ground”与完整GEAR，清晰展示了干扰惩罚的重要性，并揭示了仅奖励关键证据可能适得其反（鼓励整体复制）。
    - 对奖励系数α和β进行敏感度分析，验证了默认参数的合理性及β不能过小。
    - 对n-gram大小（n=3, 5, 10）进行消融，证实训练时使用较小n-gram（如3-gram）效果最优。
- **更深入的机理分析**：论文不仅报告了最终准确率，还直接测量了推理轨迹的重叠率和思考长度，验证了GEAR确实通过减少复制和思考长度来提升性能，而非其他因素。通过对GSM-Infinite按问题难度分组的分析（表5），排除了“高复制率是由于问题更难”这一混淆因素。
- **结论**：实验设计全面、系统，消融实验紧密围绕核心假设，结论具有说服力。评估基准的多样性和与训练数据的不重叠，保证了方法的泛化能力。实验是客观和公平的。

### 6. 主要结论与发现

1.  **问题普遍性**：重复复制是长上下文推理大语言模型中一个普遍且严重的问题，在7个前沿模型上均得到验证，且随上下文长度加剧。
2.  **核心根源**：问题的根源是**扎根不足**，即模型不加区分地进行复制，而非聚焦于任务相关的关键证据。
3.  **GEAR有效**：论文提出的GEAR方法能显著且一致地提升长上下文推理性能，在5个基准上平均提升高达+4.6个点，且收益在更长的上下文（128k）中更明显。
4.  **双重机制**：GEAR的成功在于其**组合设计**。仅使用“扎根奖励”会鼓励模型进行更多的整体复制，反而有害；只有同时使用“干扰惩罚”抑制对无关信息的复制，才能有效引导模型进行选择性聚焦。
5.  **泛化能力**：GEAR在32k上下文中训练，在128k上下文中表现出更大的提升，证明了其学习到的“证据扎根”行为具有很强的泛化能力。

### 7. 优点：方法或实验设计上的亮点

- **问题诊断深刻**：论文不仅描述了“重复复制”这一现象，还通过精细的实验设计（分离关键和干扰信息），将其根本原因归纳为“扎根不足”，为后续方法提供了坚实的理论基础。
- **方法简洁有效**：GEAR方法基于简单的n-gram统计，无需外部验证器或检索模块，易于实现且计算开销小，但性能提升显著。
- **数据构建自动化**：提出的自动数据构建流水线，使得方法能够突破合成数据的限制，扩展到更普适的自然语言场景，实用性强。
- **实验分析全面且透彻**：论文不仅展示了最终结果，还通过“基线→GSPO→GSPO+R_ground→GEAR”的轨迹分析、对思考长度和重叠率的定量测量，以及对模型行为的案例分析，完整揭示了方法的工作原理。这种“诊断+治理+验证”的闭环研究范式具有很好的参考价值。

### 8. 不足与局限

- **n-gram粒度限制**：GEAR的奖励信号基于n-gram重叠，这本质上是一种基于字面匹配的方法。对于需要深度语义理解、信息重述或逻辑推理的任务，n-gram重叠可能无法精确衡量模型是否真正“理解”并“运用”了证据，可能存在语义匹配与字面匹配的偏差。
- **训练数据范围**：训练数据最大上下文为32k，虽然实验证明了其在128k上的泛化能力，但面对更极端的上下文长度（如数百万tokens）时，GEAR的效果有待进一步验证。另外，数据构建中要求答案为“单一值”，这可能限制了其在需要生成长文本答案的复杂任务上的应用。
- **计算开销**：虽然在方法上计算开销不大，但获取用于训练数据构建的大语言模型（生成证据和验证）及进行强化学习（GSPO）本身需要大量计算资源。论文未提及具体算力，但这通常是此类研究的实际限制因素。
- **领域泛化风险**：虽然评测了多个基准，但主要集中于问答和检索类任务。在其他需要长上下文的领域（如长文档摘要、对话历史理解、代码理解），GEAR的有效性有待评估。其对“干扰”的定义也较为宽泛，在更结构化的信息环境中，干扰与证据的边界可能更加模糊。
- **潜在的反面效果**：过强的“干扰惩罚”可能会抑制模型在必要时回顾或引用上下文，甚至导致模型过度谨慎，在某些需要全局理解所有信息的任务上，可能会遗漏分散在不同位置的微妙线索。

（完）
