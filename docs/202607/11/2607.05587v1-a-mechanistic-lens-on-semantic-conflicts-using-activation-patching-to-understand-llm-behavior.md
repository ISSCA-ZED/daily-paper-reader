---
title: "A Mechanistic Lens on Semantic Conflicts: Using Activation Patching to Understand LLM Behavior"
title_zh: 语义冲突的机制视角：使用激活修补理解LLM行为
authors: "Youssef Abdelsalam, Norman Peitek, Anna-Maria Maurer, Marvin Wyrich, Sven Apel"
date: 2026-07-06
pdf: "https://arxiv.org/pdf/2607.05587v1"
tags: ["query:key-tokens"]
score: 8.0
evidence: 使用激活补丁对令牌级影响进行因果干预
tldr: 在软件工程任务中，LLM常需同时处理代码和非执行语义线索（如注释），二者可能冲突。本研究构建45个Python三元组，通过激活修补机制分析四个开源LLM在输出预测和单元测试生成中的行为。结果表明语义冲突显著降低正确性，LLM更易被误导；关键因果信息集中在少数残差流状态中。该工作提供了首个可控的机械论分析框架，揭示LLM如何整合代码语义线索。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.05587v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1853, \"height\": 903, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.05587v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 923, \"height\": 302, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.05587v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 928, \"height\": 341, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.05587v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 902, \"height\": 275, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.05587v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 923, \"height\": 300, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.05587v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 922, \"height\": 273, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.05587v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 931, \"height\": 322, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.05587v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 930, \"height\": 320, \"label\": \"Table\"}]"
motivation: 语义冲突（代码与注释等不一致）如何影响LLM行为尚不清楚，需从因果机制层面探究哪类信息主导输出。
method: 构造45个token对齐的Python三元组，控制语义线索与实现变化；结合行为评估与残差流激活修补定位因果贡献状态。
result: 语义冲突使输出预测和测试生成正确率显著下降；LLM常遵循误导性语义线索；冲突相关因果信号集中于少数中间token和变化区域。
conclusion: 语义冲突系统性损害LLM代码理解，关键信息可恢复于少量残差流状态，所提框架可推广至其他语义变异分析。
---

## 摘要
大型语言模型（LLM）越来越多地被用于处理可执行代码和不可执行语义线索（如注释或标识符）的软件工程任务。当语义线索提示的程序行为与代码本身不同时，这两类来源可能产生冲突。目前尚不清楚这种语义冲突如何影响LLM行为，以及哪类来源主导其输出。我们首次对语义冲突下LLM行为进行了受控的机制研究。为此，我们构建了45个Python代码片段三元组，通过改变语义线索或实现来隔离冲突，同时保留用于因果干预的token对齐对。我们在两个任务（输出预测和单元测试生成）上评估了四个开放权重的LLM，使用行为性能指标和残差流激活修补来识别在因果上导致对齐输入与冲突输入之间行为差异的token层状态。我们的结果表明，语义冲突显著降低了两个任务中基于执行的正确性，且所有测试的LLM经常遵循误导性的语义线索。残差流激活修补揭示了最终输出预测的一致模式：改变的线索/代码区域和一小部分中间token携带了大部分可恢复的因果信号，这些信号在输出读取附近聚合。对于单元测试生成，这一模式扩展到了提示之外，表明在生成位置产生期望值之前，冲突相关信息是可恢复的。总体而言，我们的发现表明语义冲突影响程序理解和下游任务，相关信息集中在少数因果活跃的残差流状态中，并展示了一个在受控语义变化下机制性地分析LLM如何整合代码相关信息的框架。

## Abstract
Large language models (LLMs) are increasingly used in software-engineering tasks processing executable code and non-executable semantic cues such as comments or identifiers. These two sources can conflict when semantic cues suggest different program behavior than the code itself. It remains unclear how such semantic conflicts affect LLM behavior and which source dominates their outputs.   We present the first controlled, mechanistic study of LLM behavior under semantic conflicts. To this end, we construct 45 Python snippet triplets that isolate conflicts by varying either semantic cues or implementation while keeping token-aligned pairs for causal intervention. We evaluate four open-weight LLMs on two tasks (output prediction and unit-test generation) using behavioral performance measures and residual-stream activation patching to identify token-layer states that causally contribute to behavioral differences between aligned and conflicting inputs.   Our results show that semantic conflicts significantly reduce execution-grounded correctness in both tasks and that all tested LLMs often follow misleading semantic cues. Residual-stream activation patching reveals a consistent pattern for final-output prediction: The changed cue/code region and a small set of intermediate tokens carry most of the recoverable causal signal before aggregation near the output readout. For unit-test generation, this pattern extends beyond the prompt, showing that conflict-related information is recoverable at generated sites before producing expected values. Overall, our findings show that semantic conflicts affect program comprehension and downstream tasks, with relevant information concentrated in a small number of causally active residual-stream states, and demonstrate a framework for mechanistically analyzing how LLMs integrate code-related information under controlled semantic variations.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

大型语言模型（LLM）在软件工程任务（如测试生成、程序修复、智能体开发）中需要同时处理**可执行代码**和**不可执行语义线索**（如注释、标识符等自然语言文本）。在理想情况下，这两类信息一致；但在实践中，由于文档过时、标识符误导或实现演变，它们经常出现**语义冲突**——即语义线索暗示的程序行为与代码实际行为不一致。这种冲突具有内在歧义性，因为哪类信息更可靠并无绝对标准。

**核心研究问题**：当语义冲突发生时，LLM在内部如何整合或权衡这两类信息？哪类信息主导输出？冲突如何影响下游任务（如单元测试生成）？仅通过行为输出分析无法揭示内部表示机制，因此需要结合**机械论可解释性**方法进行因果分析。

## 2. 论文提出的方法论：核心思想、关键技术细节

### 核心思想
作者提出一种结合**行为评估**与**残差流激活修补**的实验框架，通过构造token对齐的代码三元组（对齐基线、语义线索变异、实现变异），在LLM内部定位对冲突信息因果贡献最大的token-层状态。

### 关键技术细节
- **数据集构造**：45个Python片段三元组（共135个片段）。每个三元组由一个**对齐基线**（语义线索与实现一致）和两个**冲突变体**组成：
  - **线索变异**：仅修改语义线索（如将注释“even”改为“odd”），保持实现不变。
  - **实现变异**：仅修改代码实现（如将“== 0”改为“== 1”），保持语义线索不变。
  - 所有变体之间保持**token对齐**（修改部分的token数量和位置一致），以便进行残差流干预。
- **任务设计**：
  - **任务1（RQ1）**：最终输出预测——给定片段，预测其输出。
  - **任务2（RQ2）**：单元测试生成——要求生成3个pytest风格的断言。
- **残差流激活修补**：
  - 在每对对齐/冲突片段之间分别运行LLM，缓存各层每个token的残差流激活。
  - 将源片段（如冲突）的某一token-层激活替换到目标片段（如对齐）的对应位置，然后观察模型输出倾向是否向源方向偏移。
  - 计算**恢复分数**（recovery score）：衡量修补后输出logits向源行为接近的程度。设置阈值（≥0.3）筛选有显著因果贡献的修补位点。
  - 识别三类关键位点：**改变区域**（修改的token）、**载体令牌**（中间token）、**读出位点**（最后一个token）。
- **统计方法**：
  - 行为比较：配对McNemar检验（二值正确/错误）和Wilcoxon符号秩检验（断言通过率），Holm校正多重比较，报告效应量Cohen's g或rrb。

## 3. 实验设计：数据集/场景、基准、对比方法

### 数据集/场景
- **自建数据集**：45个Python片段三元组，每个短函数包含有意义的标识名和可能的注释。片段涵盖简单数学判断（如奇偶性）、字符串操作等，确保冲突对至少在一个LLM上产生行为差异。
- **任务场景**：输出预测（程序理解）和单元测试生成（下游任务）。

### 基准
- **行为基线**：每个片段的对齐版本作为“正确”基准（执行结果），冲突版本与之对比。
- **对比条件**：线索变异 vs. 实现变异，以及正向修补和反向修补（方向对称性检验）。

### 对比方法
- 本文未直接对比其他方法，而是聚焦于因果分析框架本身。相关工作的对比在讨论中提及，如与稀疏自编码器、电路追踪等方法的关系和互补性。

## 4. 资源与算力

文中未明确说明使用的GPU型号、数量、训练时长等算力详情。仅提到使用TransformerLens库进行机械论分析，以及四个约7-8B参数的LLM（CodeLlama-7B-Python, Qwen2.5-7B, Mistral-7B, Llama-3.1-8B）。由于是推理和激活修补，而非训练，算力需求相对可控，但未给出具体硬件配置。

## 5. 实验数量与充分性

### 实验数量
- **行为实验**：4个LLM × 2个冲突类型（线索/实现变异） × 2个任务 = 16个主要比较。每个比较基于45个三元组（135个片段），统计检验充分。
- **激活修补实验**：
  - RQ1.2：720个patch run（45对 × 4个LLM × 2个方向 × 2个任务？实际每个三元组有多个patch位点，最终识别2640个载体令牌）。
  - RQ2.2：针对每个生成测试用例中可区分的断言进行修补，数量取决于生成结果。
- **敏感性分析**：对修补阈值（0.2、0.3、0.5）和logit间隔阈值（0.05、0.10、0.25）进行了健壮性检查，结果在附录中报告。

### 充分性
- **优点**：配对设计有效控制片段难度差异，统计方法严谨（非参数检验、多重校正、效应量报告）。机械论分析包含正向和反向修补，验证模式稳定性。
- **不足**：仅45个片段，规模较小；仅4个LLM（7-8B），未覆盖更大模型或不同微调类型；任务聚焦于短函数，未涉及多文件、API调用、复杂副作用等真实场景。

## 6. 论文的主要结论与发现

1. **语义冲突显著降低行为正确性**（RQ1.1, RQ2.1）：
   - 输出预测正确率平均下降39.7个百分点（所有LLM-冲突对均显著，p<0.001）。
   - 单元测试断言通过率下降18.5~43.9个百分点（显著，效应量大）。
   - 许多错误输出/断言是**线索一致**的（即LLM遵循误导性语义线索），而非随机错误。

2. **激活修补揭示稳定的因果定位模式**：
   - **输出预测**（RQ1.2）：冲突信息在早期层（约第2层）出现在改变区域，中间层（约10-11层）出现在稀疏的载体令牌（平均3.67个），最后在读出位点（约23-26层）聚合。
   - **单元测试生成**（RQ2.2）：扩展到四阶段——改变区域（早期）→ 提示载体（约8-10层）→ 生成断言载体（约11-13层）→ 期望值读出位点（最后层）。
   - 恢复强度：改变区域和读出位点最高（中位数约1.0），载体令牌较低（约0.52/0.54），但载体令牌数量稀少且层位明确。

3. **语义冲突的信息集中在少量因果活跃的残差流状态中**，这为后续更细粒度的电路分析（如路径修补）缩小了搜索空间。

4. **实践启示**：过时注释、误导性标识符不仅影响人类理解，还会主动引导LLM产生错误代码、测试等，在AI辅助开发中构成风险。提出的因果分析框架可用于检测和监控此类冲突。

## 7. 优点：方法或实验设计上的亮点

- **首次将因果机械论可解释性方法系统应用于软件工程中的语义冲突问题**，填补了仅依赖行为评估的空白。
- **数据集构造精巧**：token对齐的配对设计使得残差流修补能够精确对应修改位置，避免混杂变量。
- **双任务设计**：同时覆盖直接理解（输出预测）和下游生成（单元测试），增强结论的泛化性。
- **正向/反向修补对称性**：验证了因果定位模式不依赖修补方向，增强可靠性。
- **统计严谨**：使用配对非参数检验，报告效应量和多重比较校正，敏感性分析覆盖多个阈值。
- **清晰的阶段模式**：识别出改变区域→载体令牌→读出位点的三/四阶段定位，为后续电路发现提供明确起点。

## 8. 不足与局限

- **数据集规模小，多样性有限**：仅45个Python短函数，冲突设计刻意简洁，未覆盖大型文件、多函数依赖、API调用、边条款式等现实复杂冲突。
- **模型覆盖窄**：仅4个7-8B开放权重LLM，未测试更大模型（如70B）或闭源模型（如GPT-4），也未涉及代码微调变体（如StarCoder、DeepSeek-Coder等）。结论可能随模型规模或训练数据变化。
- **任务局限**：输出预测和单元测试生成仅代表部分软件工程任务；对于代码生成、修复、审查、智能体等更开放、交互式的任务，冲突影响可能不同。
- **修补粒度粗**：残差流层-令牌粒度未区分注意力头、MLP等子组件；未追踪信息传播的具体路径（如路径修补）。
- **潜在偏差**：片段构造要求每个三元组至少在一个LLM上产生行为对比，可能导致样本选择偏差；非区分性断言（两者一致）被排除在修补分析之外，可能遗漏部分冲突信息。
- **无独立验证集**：实验使用的是自建数据集，缺乏第三方基准验证其代表性。

---

（完）
