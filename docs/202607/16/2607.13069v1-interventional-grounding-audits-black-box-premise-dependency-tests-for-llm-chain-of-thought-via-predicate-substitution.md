---
title: "Interventional Grounding Audits: Black-Box Premise-Dependency Tests for LLM Chain-of-Thought via Predicate Substitution"
title_zh: 干预性基础审计：通过谓词替换对LLM链式思维进行黑箱前提依赖性测试
authors: Hironao Nakamura
date: 2026-07-11
pdf: "https://arxiv.org/pdf/2607.13069v1"
tags: ["query:key-tokens"]
score: 8.0
evidence: 通过前提因果干预测试推理步骤的依赖性
tldr: "大语言模型在链式推理中可能产生看似逻辑连贯但实际不依赖前提的推理步骤。我们提出了介入式归因审计，这是一种黑盒步骤级测试，通过用新鲜符号替换前提中的目标谓词，重新运行模型并检查结论变化。在ProntoQA合成多跳推理基准上，GPT-4o达到F1=0.806，远高于自一致性基线（0.343），并发现66%的正确解答中至少有一个步骤对直接证明树依赖不敏感，尤其涉及实体引入前提这一已知盲点。该方法能识别“正确但错误推理”盲点，为评测模型推理忠实性提供可复现的审计工具。"
source: arxiv
selection_source: fresh_fetch
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.13069v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 869, \"height\": 298, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.13069v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 662, \"height\": 317, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.13069v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 448, \"height\": 206, \"label\": \"Table\"}]"
motivation: 现有CoT评估多关注答案正确性，忽略推理步骤对前提的真实依赖，导致“正确但错误推理”难以被发现，需要黑盒、步骤级的依赖审计方法。
method: 通过替换前提中的谓语为新鲜符号，黑盒测试每个推理步骤的结论是否变化，从而判定该步骤对前提的依赖性。
result: "在ProntoQA上，GPT-4o检测证明树依赖F1=0.806（谓语依赖F1=0.885，召回100%），远超自一致性基线（0.343），并发现66%正确解答存在依赖盲点。"
conclusion: 该方法能暴露模型推理中的虚假前提依赖，为提升LLM推理忠实性提供新评估范式，代码已开源。
---

## 摘要
大型语言模型生成的链式思维（CoT）推理在逻辑上看似合理，但可能并不真正依赖于其陈述的前提。我们引入了干预性基础审计，这是一种黑箱的、步骤级别的前提依赖性测试方法：我们通过将其目标谓词替换为一个新符号来干预单个前提，重新运行模型，并检查每个推理步骤的归一化结论（规范谓词形式）是否发生变化。我们在ProntoQA（一个具有黄金证明树的合成多跳演绎推理基准，其中已知步骤级别的前提依赖性）上进行了评估。将我们的方法应用于50个ProntoQA问题（使用GPT-4o），在检测证明树依赖性上实现了F1=0.806（谓词决定性依赖性的F1=0.885；召回率=100%），显著优于自一致性基线（F1=0.343；95%自助法置信区间无重叠）。我们进一步发现，在正确解决的问题中，有66%包含至少一个对齐的步骤，该步骤在一致替换下对直接证明树依赖性不敏感——所有这些都涉及实体引入前提，这是一致替换评估器已知的盲点——这是一种被动方法无法发现的“正确答案，错误推理”信号。所有审计证书、原始输出和复现脚本均可在公共GitHub仓库中获取，我们讨论了超出形式化、可解析基准的范围限制。

## Abstract
Large language models produce chain-of-thought (CoT) reasoning that appears logically sound yet may not genuinely depend on its stated premises. We introduce interventional grounding audits, a black-box, step-level test of premise dependency: we intervene on a single premise by substituting its target predicate with a fresh symbol, re-run the model, and check whether each reasoning step's normalized conclusion (canonical predicate form) changes. We evaluate on ProntoQA, a synthetic multi-hop deductive reasoning benchmark with gold proof trees, where step-level premise dependencies are known. Applied to 50 ProntoQA problems with GPT-4o, our method achieves F1 = 0.806 on detecting proof-tree dependencies (F1 = 0.885 on predicate-determining dependencies; Recall = 100%), significantly outperforming a self-consistency baseline (F1 = 0.343; 95% bootstrap CIs non-overlapping). We further identify that 66% of correctly-solved problems contain at least one aligned step insensitive to a direct proof-tree dependency under consistent substitution -- all involving entity-introduction premises, a documented blind spot of the consistent-substitution evaluator -- a "right answer, wrong reasoning" signal invisible to passive methods. All audit certificates, raw outputs, and reproduction scripts are available in a public GitHub repository, and we discuss scope limits beyond formal, parsable benchmarks.

---

## 论文详细总结（自动生成）

## 1. 核心问题与整体含义
- **研究动机**：大语言模型（LLM）生成的链式思维（CoT）推理在逻辑上看似合理，但可能并不真正依赖于它所陈述的前提。现有评估方法（如仅检查最终答案正确性、自一致性、注意力分析等）属于被动观察，无法区分真正的逻辑依赖与表面相关，从而可能遗漏“正确答案，错误推理”（RAWR）信号。  
- **整体意义**：论文旨在建立一个黑箱、步骤级的因果依赖测试，通过干预前提中的谓词来判定CoT中每个步骤是否确实依赖其声称的前提，从而提高LLM推理的忠实性和可审计性。

## 2. 方法论
- **核心思想**：干预性基础审计。通过替换前提中的目标谓词（如将“tumpus”替换为“glumpus”），重新运行模型，观察每个推理步骤的归一化结论是否发生变化。若变化，则该步骤真正依赖于该前提；否则不依赖。
- **关键技术细节**：
  - **两种替换策略**：
    - **一致替换**：在所有前提中替换目标谓词，保持链条一致性，用于检测谓词决定性依赖。
    - **局部替换**：仅在被测试前提中替换，故意打破链条，用于检测传递性依赖（包括结构前提如实体-介绍）。
  - **控制探针**：对前提进行表面重述（如“All X are Y”→“Every X is a Y”），以区分输出变化是源于表面敏感还是逻辑依赖。
  - **归一化结论提取**：将每个步骤的结论解析为规范形式（`is(e, p)` 或 `subtype(p1, p2)`），消除措辞变异。
  - **五值判定**：针对每个步骤-前提对，比较原始、语义探针和表面探针下的结论输出，得到：GROUNDED（逻辑依赖）、INSENSITIVE（无依赖）、INPUT-SENSITIVE（对任何变化都敏感）、UNSTABLE（仅对表面变化敏感）、UNPARSEABLE（解析失败）。
  - **级联检测与过滤**：对于局部替换引发的传播假阳性，若上游步骤已被判定为GROUNDED，则当前步骤重新分类为CASCADE，从而恢复精确率。
- **算法流程**（文字描述）：
  1. 对每个前提Pj，生成三种变体（语义替换、局部替换、表面重述）。
  2. 将原始前提集合和三种变体分别输入LLM，获得CoT输出。
  3. 解析所有CoT步骤的结论，归一化为规范形式。
  4. 对每个步骤Si与每个前提Pj，根据原始与变体下的结论比较，得到五值判定。
  5. 应用级联过滤规则修正部分判定。
  6. 输出审计证书包含每个判定的依据（SHA256校验）。

## 3. 实验设计
- **数据集与基准**：使用ProntoQA（合成多跳演绎推理基准），包含50个问题（3–5跳，4–7前提，True/False平衡），每个问题具有黄金证明树，注明每个步骤的直接前提依赖。
- **目标模型**：GPT-4o（快照`gpt-4o-2024-08-06`，temperature=0）。额外跨模型分析使用Claude Sonnet 4.5（`claude-sonnet-4-5-20250929`）。
- **对比方法**：
  - **自一致性基线**：通过5次采样检查答案一致性，但无法识别步骤级依赖（F1=0.343，精确率仅0.226）。
  - **字符串差异基线**：直接比较原始与替换后CoT字符串的差异（F1=0.787）。
- **评估指标**：精确率、召回率、F1，基于证明树真值（直接依赖）。使用95%百分位自助法置信区间（B=10,000，按问题重采样）。两种评价模式：F1 full（所有依赖）、F1 pred（仅谓词决定性依赖）。

## 4. 资源与算力
- **明确说明**：论文没有提及使用的GPU型号、数量或训练时长。所有实验均通过API调用GPT-4o和Claude Sonnet 4.5完成，未涉及本地训练或微调。因此计算资源细节未披露。

## 5. 实验数量与充分性
- **实验数量**：主要实验在50个ProntoQA问题上进行，生成1,127个审计证书，其中1,031个成功对齐至证明树步骤。进行了多组分析：
  - 主要结果表（表1）：对比本方法与自一致性、字符串差异。
  - 消融研究（表2）：评估一致替换、局部替换、级联过滤、表面控制等组件的贡献。
  - 链长度分析（表3）：3–5跳的F1变化。
  - 误报细粒度分析：40个非依赖候选的构成。
  - RAWR分析：计算正确解决问题中敏感步骤的比例。
  - 跨模型验证：在Claude Sonnet 4.5上进行完整复现。
- **充分性与公平性**：实验设计较为充分，包含了统计显著性检验（非重叠CI）、消融实验、跨模型验证和错误分析。但数据集规模仅50个问题，且仅限于形式化合成基准，可能限制泛化性。作者也承认这一点，并通过bootstrap CI论证统计有效性。对比方法（自一致性）在任务（检测前提依赖）上本质上较弱，但论文给出了公平比较。

## 6. 主要结论与发现
- **主结果**：干预性基础审计在全依赖检测上F1=0.806，显著优于自一致性（F1=0.343）；在谓词决定性依赖上F1=0.885，召回率达到100%。
- **级联过滤**：结合一致与局部替换并添加级联过滤，F1提升至0.819，精确率从0.604恢复至0.756。
- **RAWR现象**：66%的正确解决问题中包含至少一个步骤对直接证明树依赖不敏感（全部为实体-介绍前提），自一致性无法发现这些“正确但错误推理”信号。
- **误述检测**：发现了30个案例，模型在文本中引用该前提作为依据，但实际对该前提不敏感。
- **跨模型可迁移性**：在Claude Sonnet 4.5上审计成功转移（F1=0.872，精确率100%），但在规则化输出上字符串差异基线也达到了F1=0.874，说明优势依赖于模型输出风格。

## 7. 优点
- **创新性**：首次提出黑箱、步骤级、因果化的前提依赖测试，不需要模型内部结构，具有通用性。
- **可复现性**：完整发布审计证书、原始输出、源代码和验证脚本（包含SHA256校验），审稿人无需API即可复现所有结果。
- **严谨的基准与统计**：使用黄金证明树作为真值，提供bootstrap置信区间，确保结果可靠性。
- **多层面消融与控制**：包含一致/局部替换、表面控制、级联过滤，清晰展示了各组件的贡献。
- **实际诊断价值**：能识别RAWR和误述，这对于评估模型推理忠实性具有实际意义。
- **理论框架连接**：将方法嵌入两拓扑基础框架（Nakamura 2025, 2026a），提供了理论支撑。

## 8. 不足与局限
- **实验范围有限**：仅在一个合成形式化基准（ProntoQA，50个问题）上评估，缺乏自然语言逻辑数据集（如FOLIO、ProofWriter）的验证，泛化性未证实。
- **解析覆盖率不完全**：约10%的证书因无法解析或对齐而被排除，可能引入选择偏差。下界分析（将所有排除视为负例）显示F1=0.703，仍显著优于基线，但表明性能有所下降。
- **计算资源未报告**：未披露API调用成本或本地计算开销，不利于估算可扩展性。
- **自一致性基线在任务上天然劣势**：自一致性方法本身并非为步骤级依赖检测设计，对比可能不够“公平”，但论文已澄清其目的是展示被动方法的不足。
- **跨模型结论的稳定性**：在Claude Sonnet 4.5上字符串差异F1=0.874几乎持平，表明在规则化输出中简单基线可能足够，审计优势取决于模型输出变化程度。
- **盲区**：一致替换对实体-介绍前提（结构前提）失效，需要依赖局部替换；且局部替换的级联假阳性仍需过滤。
- **仅测试两个商业模型**：未涉及开源模型或更小规模的LLM，结论的广度受限。

（完）
